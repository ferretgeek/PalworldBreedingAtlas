# 项目协作规则

- 开始工作前阅读 `README.md`、`docs/开发维护手册.md` 与 `docs/数据来源与更新.md`。
- 面向简体中文 Windows 11；桌面运行时完全离线，存档始终只读。
- 正式默认图鉴必须严格为 287 条；数据变更先改生成器，再运行 `scripts/verify.ps1`。
- 当前世界库存、跨界基因与未知单位必须保持分离；已出售、删除或空槽不得重新计入。
- Tkinter 工作线程不得直接更新控件；后台结果必须校验 generation/cancel。
- UI 沿一套深灰暗色与三套完整浅色主题，以及“选目标 → 设条件 → 生成路线”信息架构演进；不删除空、错、忙、禁用、缓存恢复、键盘、减少动态效果与移动端状态。
- 不引入遥测、隐藏常驻进程、远程自更新或运行时网络依赖。
- 不提交 `.sav`、本机路径、运行缓存、生成网页、日志、私人测试夹具或 `oo2core_*_win64.dll`。
- 原创代码与第三方素材的许可边界见 `THIRD_PARTY_NOTICES.md`；不得把第三方素材标记为 MIT。
- 交付前运行 `scripts/verify.ps1`，并实际检查桌面与 390px 窄屏。
